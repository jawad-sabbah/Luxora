🔹 1. Core Features

Here are the typical features a hotel booking website should have:

Guest Features

🏨 Browse hotels & rooms (search by location, date, price, amenities).

📅 Check availability of rooms.

🛏️ View room details (photos, price, amenities, reviews).

📝 Register & login (user account).

❤️ Save favorites (wishlist).

💳 Book rooms & make payments.

⭐ Add reviews after checkout.

📖 View booking history.

Host/Admin Features

👨‍💼 Host registration & verification.

🏠 Add/manage properties (hotels, apartments).

🚪 Add/manage rooms (availability, pricing, photos).

📊 Dashboard with stats (bookings, revenue).

🔄 Approve/Reject bookings.

🧾 Manage payments & refunds.

📝 Manage reviews.

Super Admin

👑 Manage all users & hosts.

🚫 Block/unblock accounts.

📊 Full site analytics.





        <!-- Gallery -->
        <h3 class="text-xl font-semibold text-gray-800 mb-4">Gallery</h3>
        <div class="grid grid-cols-2 md:grid-cols-3 gap-4 mb-8">
          <% hotel.gallery.forEach(img => { %>
            <img src="<%= img %>" class="rounded-lg w-full h-40 object-cover" alt="Gallery image">
          <% }) %>
        </div> 

        




           <!-- Recent Bookings Table -->
    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="text-xl font-semibold text-gray-800 mb-4">Recent Bookings</h2>
      <table class="min-w-full border divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-4 py-2 text-left text-gray-600">Booking ID</th>
            <th class="px-4 py-2 text-left text-gray-600">User</th>
            <th class="px-4 py-2 text-left text-gray-600">Hotel</th>
            <th class="px-4 py-2 text-left text-gray-600">Room</th>
            <th class="px-4 py-2 text-left text-gray-600">Status</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-100">
          <% recentBookings.forEach(booking => { %>
            <tr>
              <td class="px-4 py-2"><%= booking.id %></td>
              <td class="px-4 py-2"><%= booking.user_name %></td>
              <td class="px-4 py-2"><%= booking.hotel_name %></td>
              <td class="px-4 py-2"><%= booking.room_type %></td>
              <td class="px-4 py-2">
                <% if (booking.status === 'confirmed') { %>
                  <span class="text-green-600 font-semibold">Confirmed</span>
                <% } else if (booking.status === 'pending') { %>
                  <span class="text-yellow-600 font-semibold">Pending</span>
                <% } else { %>
                  <span class="text-red-600 font-semibold"><%= booking.status %></span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
      
