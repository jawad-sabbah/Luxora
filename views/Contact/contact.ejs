<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Contact Us - Luxora</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

  <!-- Navbar (optional) -->
  <header class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
      <a href="/" class="text-2xl font-bold text-blue-600">Luxora</a>
      <nav>
        <a href="/" class="mx-3 text-gray-700 hover:text-blue-600">Home</a>
        <a href="/hotels" class="mx-3 text-gray-700 hover:text-blue-600">Hotels</a>
        <a href="/about" class="mx-3 text-gray-700 hover:text-blue-600">About</a>
        <a href="/contact" class="mx-3 text-blue-600 font-semibold">Contact</a>
        <a href="/login" class="mx-3 text-gray-700 hover:text-blue-600">Login</a>
      </nav>
    </div>
  </header>

  <!-- Main Contact Section -->
  <main class="flex-grow flex items-center justify-center px-6 py-12">
    <div class="w-full max-w-4xl bg-white rounded-2xl shadow-lg p-8 grid md:grid-cols-2 gap-8">

      <!-- Left: Contact Info -->
      <div>
        <h2 class="text-3xl font-bold text-gray-800 mb-4">Get in Touch</h2>
        <p class="text-gray-600 mb-6">
          Have questions about bookings, hosting, or payments?  
          Reach out to us anytime – we’d love to help!
        </p>

        <div class="space-y-4">
          <p class="flex items-center">
            <span class="w-8 h-8 flex items-center justify-center bg-blue-100 rounded-full mr-3">📍</span>
            Beirut, Lebanon
          </p>
          <p class="flex items-center">
            <span class="w-8 h-8 flex items-center justify-center bg-blue-100 rounded-full mr-3">📞</span>
            +961 123 456 789
          </p>
          <p class="flex items-center">
            <span class="w-8 h-8 flex items-center justify-center bg-blue-100 rounded-full mr-3">✉️</span>
            support@Luxora.com
          </p>
        </div>
      </div>

      <!-- Right: Contact Form -->
      <div>
        <form action="/contact" method="POST" class="space-y-5">
          <!-- Name -->
          <div>
            <label class="block mb-1 font-medium">Full Name</label>
            <input type="text" name="name" required
              class="w-full px-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:outline-none"
              placeholder="John Doe">
          </div>

          <!-- Email -->
          <div>
            <label class="block mb-1 font-medium">Email</label>
            <input type="email" name="email" required
              class="w-full px-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:outline-none"
              placeholder="you@example.com">
          </div>

          <!-- Message -->
          <div>
            <label class="block mb-1 font-medium">Message</label>
            <textarea name="message" rows="4" required
              class="w-full px-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:outline-none"
              placeholder="Your message..."></textarea>
          </div>

          <!-- Submit -->
          <button type="submit"
            class="w-full bg-blue-600 text-white py-3 rounded-xl font-semibold hover:bg-blue-700 transition">
            Send Message
          </button>
        </form>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-6 text-center">
    <p>© <%= new Date().getFullYear() %> Luxora. All rights reserved.</p>
  </footer>
</body>
</html>
